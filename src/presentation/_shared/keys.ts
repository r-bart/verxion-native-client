// Re-export query keys used across feature boundaries.
// Features should import from here instead of reaching into each other's keys.ts
export { todayKeys } from "@/presentation/today/keys";
export { progressKeys } from "@/presentation/progress/keys";
