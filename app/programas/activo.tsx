import { ProgramDetailScreen } from "@/presentation/program/screens/ProgramDetailScreen";

// Detalle del programa activo (entrada desde Hoy) — sin id → resuelve el activo.
export default function ProgramaActivoRoute() {
  return <ProgramDetailScreen />;
}
